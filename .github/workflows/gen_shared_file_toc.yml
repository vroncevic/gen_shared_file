name: gen_shared_file_toc
on:
  push:
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: gen_shared_file_toc
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
